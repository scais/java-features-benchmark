package fundamentals.operators;

public class TernaryOperator {

    void ternarySimple(int bar) {
        int foo = (bar == 10) ? 0 : 1;
    }

    int returnFromTernary(int bar) {
        return (bar == 10) ? 0 : 1;
    }

}
